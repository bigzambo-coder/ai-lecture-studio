# AI LECTURE STUDIO - Codex Project Instructions

이 프로젝트는 문정수 강사의 강의 기획·제작 워크플로우를 앱으로 구현한다.

작업을 시작하기 전에 반드시 다음 파일을 순서대로 읽는다.

1. `requirements.md` - 앱 목적, 사용자, 기능, 화면, 데이터 요구사항
2. `workflow.md` - 단계, 승인, 수정 영향 관계
3. `prompts.md` - 단계별 AI 생성 프롬프트와 출력 스키마
4. `sources/AI_LECTURE_STUDIO_CODEX_MASTER_v9.md` - 전체 제작·QA 기준
5. `sources/기획서_자동생성_코덱스용.md` - 기관 A~F 자동판별과 유형별 기획서 기준
6. 작업과 관련된 `samples/` 원본만 선택적으로 분석

## 구현 원칙

- 앱은 사용자 입력부터 FINAL QA까지 단계형 프로젝트로 만든다.
- MASTER BRIEF 승인 전에는 문서 제작을 시작하지 않는다.
- 각 단계는 생성 → 파일 제작 → 렌더링 QA → 승인 대기 순서로 처리한다.
- 사용자가 `다음`을 선택해야 다음 단계로 이동한다.
- 승인된 파일은 덮어쓰지 않고 새 버전으로 저장한다.
- 상위 단계가 수정되면 영향을 받는 하위 산출물을 재검토 상태로 바꾼다.
- 샘플의 레이아웃과 구조는 참고하되 내용은 복제하지 않는다.
- 실제 파일이나 실제 Notion 페이지가 없으면 완료라고 표시하지 않는다.
- 비밀키는 코드에 직접 쓰지 말고 환경변수로 관리한다.
- 기획서는 A~F 유형을 자동 판별하되 사용자가 선택한 유형과 기관 첨부 양식을 최우선으로 적용한다.
- 확인하지 않은 날짜·장소·예산·기관 정책은 만들지 않고 `[직접 입력]` 또는 `[기관 확인 필요]`로 표시한다.

## 우선 구현 범위

1. 프로젝트 생성과 공통 입력
2. 선택형 추가 인터뷰
3. MASTER BRIEF 확인·수정·승인
4. 단계별 생성과 승인 상태관리
5. 산출물 미리보기·다운로드·버전관리
6. 수정 영향도와 FINAL QA

## 산출물 기준

- 강의계획서: DOCX, 선택 PDF
- 교육제안서: DOCX, 선택 PDF
- 세부커리큘럼: DOCX 또는 XLSX
- PPT 설계: Markdown 또는 JSON
- 강의자료: PPTX
- 실습워크북: 실제 Notion 페이지
- 발표스크립트: DOCX

기존 요구안의 `제안서=PPTX`, `워크북=DOCX/PDF`는 현재 운영 기준과 다르므로 위 기준을 적용한다.
